version: '3'

services:

  mqtt:
    image: eclipse-mosquitto
    ports:
      - 1883:1883
      - 9001:9001
    volumes:
      - ./mosquitto/config:/mosquitto/config 
      - ./mosquitto/data:/mosquitto/data
      - ./mosquitto/config:/mosquitto/passwords    
    restart: always